#include<stdio.h>

int main()
{
    int n;

    scanf("%d",&n);

    int res[10];
int len=0;
    while(1)
    {
        res[len]=n%10;
        n/=10;
        len++;
        if(n==0)
        {
            break;
        }

    }

    printf("%d",len);
return 0;

}
